# 📜 VLESS-ключи: руководство по включению функции вывода

## ✨ Что было добавлено

Добавлена функциональность **вывода VLESS-ключей** для хостов. Теперь администраторы могут:
- ✅ Включать/отключать вывод raw VLESS-ключей для каждого хоста
- ✅ Управлять этой настройкой через Telegram бота и веб-интерфейс
- ✅ По умолчанию функция отключена (безопасное значение по умолчанию)

---

## 🔄 Что изменилось в коде

### 1. База данных (`src/shop_bot/data_manager/database.py`)

#### Добавленная колонка в таблицу `xui_hosts`:
```sql
ALTER TABLE xui_hosts ADD COLUMN vless_output_enabled INTEGER DEFAULT 0
```

**Когда автоматически добавится:** При следующем запуске приложения функция `run_migration()` автоматически добавит эту колонку если её ещё нет в таблице.

#### Новая функция: `update_host_vless_output()`
```python
def update_host_vless_output(host_name: str, enabled: bool) -> bool:
    """
    Обновить параметр отображения VLESS-ключей для хоста.
    
    Args:
        host_name (str): Имя хоста
        enabled (bool): True = включить, False = отключить
        
    Returns:
        bool: True если успешно обновлено, иначе False
    """
```

**Использование:**
```python
from src.shop_bot.data_manager.database import update_host_vless_output

# Включить вывод VLESS-ключей для хоста
update_host_vless_output("server-eu-1", True)

# Отключить вывод VLESS-ключей для хоста
update_host_vless_output("server-eu-1", False)
```

#### Обновленная функция: `create_host()`
Теперь принимает дополнительный параметр `vless_output_enabled`:
```python
def create_host(
    name: str, 
    url: str, 
    user: str, 
    passwd: str, 
    inbound: int, 
    subscription_url: str | None = None,
    vless_output_enabled: int = 0  # ← НОВЫЙ ПАРАМЕТР
):
```

---

### 2. Telegram Bot (`src/shop_bot/bot/`)

#### Обновленная клавиатура (`keyboards.py`)
Добавлена кнопка **"📜 Вывод VLESS-ключей"** в меню управления хостом:

```python
def create_admin_host_manage_keyboard(host_digest: str) -> InlineKeyboardMarkup:
    # ... существующие кнопки ...
    builder.button(text="📜 Вывод VLESS-ключей", callback_data=f"admin_hosts_toggle_vless:{host_digest}")
    # ... остальные кнопки ...
```

#### Новый обработчик (`admin_handlers.py`)
Функция `admin_hosts_toggle_vless()` обрабатывает нажатие кнопки:

```python
@admin_router.callback_query(F.data.startswith("admin_hosts_toggle_vless:"))
async def admin_hosts_toggle_vless(callback: types.CallbackQuery, state: FSMContext):
    """Переключить параметр вывода VLESS-ключей для хоста."""
    # Получает текущее значение и инвертирует его
    # Обновляет параметр в БД
    # Показывает пользователю статус
```

#### Обновленная информация о хосте
Функция `_format_host_card()` теперь показывает статус VLESS:

```
📜 Вывод VLESS-ключей: 📜 ✅ Включен
или
📜 Вывод VLESS-ключей: 📜 ❌ Отключен
```

---

### 3. Веб-интерфейс (`src/shop_bot/webhook_server/`)

#### Новый маршрут (`app.py`)
Добавлен маршрут `/update-host-vless-output`:

```python
@flask_app.route('/update-host-vless-output', methods=['POST'])
@login_required
def update_host_vless_output_route():
    """Обновить параметр вывода VLESS-ключей для хоста."""
```

#### Обновленная форма добавления хоста (`templates/settings.html`)
Добавлен checkbox **"Вывод VLESS-ключей"** при создании нового хоста:

```html
<div class="form-group" style="display: flex; align-items: center;">
    <input type="checkbox" id="vless_output_enabled" name="vless_output_enabled" style="margin-right: 8px;" />
    <label for="vless_output_enabled" style="margin-bottom: 0;">Вывод VLESS-ключей</label>
</div>
```

#### Добавлена форма переключения в редактировании хоста
На странице настроек каждого хоста добавлена форма с checkbox для управления параметром:

```html
<form action="{{ url_for('update_host_vless_output_route') }}" method="post" class="form-inline" style="margin: 8px 0;">
    <input type="hidden" name="csrf_token" value="{{ csrf_token() }}" />
    <input type="hidden" name="host_name" value="{{ host.host_name }}" />
    <div style="display: flex; align-items: center; gap: 8px;">
        <input type="checkbox" id="vless_{{ loop.index }}" name="vless_output_enabled" value="on" 
               {% if host.vless_output_enabled %}checked{% endif %} />
        <label for="vless_{{ loop.index }}" style="margin: 0;">Вывод VLESS-ключей</label>
        <button type="submit" class="btn btn-outline-secondary btn-sm pill">Сохранить</button>
    </div>
</form>
```

---

## 🚀 Как использовать

### В Telegram боте

**1. Открыть меню управления хостом:**
- Перейти в Админ-панель → Хосты → Выбрать хост

**2. Переключить вывод VLESS:**
- Нажать кнопку **"📜 Вывод VLESS-ключей"**
- Бот покажет статус (включен/отключен)

**3. При создании нового хоста:**
- В форме "Добавить новый хост" установить галочку **"Вывод VLESS-ключей"**
- Нажать кнопку "Добавить хост"

### В веб-интерфейсе

**1. Открыть страницу Настроек → Хосты**

**2. При добавлении нового хоста:**
- Заполнить все обязательные поля
- Установить галочку **"Вывод VLESS-ключей"** (опционально)
- Нажать "Добавить хост"

**3. При редактировании существующего хоста:**
- Найти хост в списке
- В секции "Вывод VLESS-ключей":
  - Установить/снять галочку
  - Нажать кнопку "Сохранить"

---

## 📊 Примеры использования

### Пример 1: Включить VLESS для хоста через Python
```python
from src.shop_bot.data_manager.database import update_host_vless_output

# Включить вывод VLESS-ключей
result = update_host_vless_output("my-host", True)
if result:
    print("✅ VLESS включен")
else:
    print("❌ Ошибка")
```

### Пример 2: Проверить статус VLESS хоста
```python
from src.shop_bot.data_manager.database import get_host

host = get_host("my-host")
if host:
    vless_status = "Включен" if host.get('vless_output_enabled') else "Отключен"
    print(f"VLESS для {host['host_name']}: {vless_status}")
```

### Пример 3: Получить все хосты с включенным VLESS
```python
from src.shop_bot.data_manager.database import get_all_hosts

hosts = get_all_hosts()
vless_hosts = [h for h in hosts if h.get('vless_output_enabled')]
for host in vless_hosts:
    print(f"📜 {host['host_name']} - VLESS включен")
```

---

## ⚙️ Интеграция с отображением ключей (УЖЕ РЕАЛИЗОВАНО ✅)

### Автоматический вывод VLESS ключей

Функциональность уже полностью интегрирована! Теперь везде, где выводится информация о ключе:
- В Telegram боте при просмотре информации о ключе
- После успешной покупки/продления
- В информации о подарочных ключах

Если в хосте ключа **включен вывод VLESS-ключей**, то после выведения обычной subscription_url будут показаны **все raw VLESS ключи (subscription URLs)** для всех пользователей этого хоста.

### Как это выглядит в Telegram боте

```
🔑 Ваш ключ: #1

┌─────────────────────────────────┐
│ 📧 Email: user@bot.local       │
│ ➕ Приобретён: 10.02.2026      │
│ ⏳ Действителен до: 10.03.2026 │
└─────────────────────────────────┘

<code>vless://uuid@host:port?...#Your%20Key</code>

📱 Вы подключили: 0

📦 Тариф ключа:
┌────────────────────────────────┐
│ 📁 Группа: 2 устройства       │
│ 🕒 Тариф: 30 дней             │
│ 📱 Лимит устройств: 2          │
└────────────────────────────────┘

📜 Raw VLESS ключи хоста:
1. <code>vless://uuid1@host:port?...#Key1</code>
2. <code>vless://uuid2@host:port?...#Key2</code>
3. <code>vless://uuid3@host:port?...#Key3</code>
```

### Реализованные функции

#### В файле `src/shop_bot/config.py`:

**Новая async функция:** `get_key_info_text_with_vless_async()`
- Получает информацию о ключе
- Если включен вывод VLESS хоста - получает все VLESS ключи через Remnawave API
- Выводит их в конце информации о ключе

**Новая async функция:** `get_purchase_success_text_with_vless_async()`
- Получает сообщение об успешной покупке/продления
- Если включен вывод VLESS хоста - получает все VLESS ключи
- Выводит их в конце сообщения

#### В файле `src/shop_bot/modules/remnawave_api.py`:

**Новая async функция:** `get_all_vless_keys_for_host()`
```python
async def get_all_vless_keys_for_host(host_id: int) -> list[str]:
    """
    Получить все raw VLESS ключи (subscription URLs) для хоста.
    
    Args:
        host_id: Числовой ID хоста (уникальный идентификатор)
        
    Returns:
        list[str]: Список VLESS subscription URLs (raw ключей)
    """
```

Эта функция:
1. Принимает **host_id** (уникальный числовой идентификатор) вместо host_name
2. Преобразует host_id в host_name (из БД) для работы с Remnawave API
3. Получает всех пользователей для хоста через `list_users()`
4. Извлекает их subscriptionUrl (raw VLESS)
5. Возвращает список всех VLESS URL'ов

**Преимущества использования host_id:**
- ✅ Уникальность (имена хостов могут совпадать)
- ✅ Быстрые поиски (индексы в БД)
- ✅ Надежность (ID не меняются)

#### В файле `src/shop_bot/bot/handlers.py`:

Обновлены вызовы в следующих функциях:
- `show_gift_handler()` - показ информации о подарочном ключе
- `show_key_handler()` - показ информации о ключе пользователя
- `select_host_for_switch()` - после переноса ключа на другой хост
- `manage_keys_handler()` - в процессе обновления инбаунда
- `trial_key_handler()` - после создания пробного ключа
- `process_successful_payment()` - после успешной оплаты

Все эти места теперь используют **async версии функций** вместо sync версий.

---

## 🔒 Безопасность

- ✅ По умолчанию функция **отключена** (значение по умолчанию = 0)
- ✅ Только администраторы могут изменять этот параметр
- ✅ Изменение требует CSRF токен (в веб-интерфейсе)
- ✅ Значение хранится в БД с типом INTEGER (0 или 1)
- ✅ VLESS ключи получаются **только** если включен вывод для хоста
- ✅ Список получается напрямую из Remnawave API (всегда актуален)

---

## 🔄 Миграция БД

Миграция автоматически запустится при следующем запуске приложения:

```python
# В src/shop_bot/data_manager/database.py функция run_migration() 
# автоматически добавит колонку если её ещё нет:

def _ensure_hosts_columns(cursor: sqlite3.Cursor) -> None:
    extras = {
        # ... другие колонки ...
        "vless_output_enabled": "INTEGER DEFAULT 0",  # ← НОВОЕ ПОЛЕ
    }
    for column, definition in extras.items():
        _ensure_table_column(cursor, "xui_hosts", column, definition)
```

---

## 📝 Изменённые файлы

### Основные файлы с функциональностью

1. ✅ `src/shop_bot/data_manager/database.py`
   - Обновлена функция `_ensure_hosts_columns()` 
   - Обновлена функция `create_host()`
   - Добавлена функция `update_host_vless_output()`

2. ✅ `src/shop_bot/bot/keyboards.py`
   - Обновлена функция `create_admin_host_manage_keyboard()`
   - Добавлена кнопка "📜 Вывод VLESS-ключей"

3. ✅ `src/shop_bot/bot/admin_handlers.py`
   - Добавлены импорты
   - Обновлена функция `_format_host_card()`
   - Добавлена функция `admin_hosts_toggle_vless()`

4. ✅ `src/shop_bot/webhook_server/app.py`
   - Добавлены импорты
   - Обновлена функция `add_host_route()`
   - Добавлена функция `update_host_vless_output_route()`

5. ✅ `src/shop_bot/webhook_server/templates/settings.html`
   - Добавлен checkbox в форму создания хоста
   - Добавлена форма управления VLESS в редактировании хоста

### Новые файлы для поддержки выведения VLESS ключей

6. ✅ `src/shop_bot/config.py` (ОБНОВЛЕНО)
   - Добавлена async функция `get_key_info_text_with_vless_async()`
   - Добавлена async функция `get_purchase_success_text_with_vless_async()`
   - Эти функции автоматически выводят VLESS ключи

7. ✅ `src/shop_bot/modules/remnawave_api.py` (ОБНОВЛЕНО)
   - Добавлена async функция `get_all_vless_keys_for_host()`
   - Получает все VLESS ключи для хоста из Remnawave API

8. ✅ `src/shop_bot/bot/handlers.py` (ОБНОВЛЕНО)
   - Добавлены импорты новых async функций
   - Обновлены 6 функций которые теперь используют async версию:
     - `show_gift_handler()`
     - `show_key_handler()`
     - `select_host_for_switch()`
     - `manage_keys_handler()`
     - `trial_key_handler()`
     - `process_successful_payment()`

---

## ✅ Тестирование

### Тестирование переключения VLESS в админ-панели

```bash
# 1. Запустить приложение (миграция выполнится автоматически)
python -m src.shop_bot

# 2. В Telegram боте:
#    - Открыть /admin
#    - Перейти в Хосты
#    - Выбрать любой хост
#    - Проверить что видна кнопка "📜 Вывод VLESS-ключей"
#    - Нажать кнопку - должна переключаться на Enable/Disable
#    - Проверить что статус обновляется

# 3. В веб-интерфейсе (http://localhost:5000):
#    - Открыть Настройки → Хосты
#    - Проверить checkbox "Вывод VLESS-ключей" при добавлении хоста
#    - В редактировании хоста должна быть форма переключения
```

### Тестирование выведения VLESS ключей

```bash
# 1. Включить "Вывод VLESS-ключей" для хоста в админ-панели

# 2. Купить или создать пробный ключ

# 3. Проверить в Telegram боте:
#    - Перейти в "Мои ключи"
#    - Нажать на любой ключ
#    - Должны увидеть все raw VLESS ключи хоста в конце информации:
#      📜 Raw VLESS ключи хоста:
#      1. <code>vless://uuid1@host:port?...#Key1</code>
#      2. <code>vless://uuid2@host:port?...#Key2</code>

# 4. Проверить в веб-интерфейсе:
#    - Перейти к пользователю и его ключам
#    - Если включен вывод VLESS, то raw ключи должны быть видны
```

---

## 🎯 Как это работает

### Алгоритм выведения VLESS ключей

1. **Когда пользователь смотрит информацию о ключе:**
   - Вызывается `get_key_info_text_with_vless_async()`
   - Она получает базовую информацию о ключе через `get_key_info_text()`
   - Проверяет найден ли хост и включен ли `vless_output_enabled`
   - Если включен, вызывает `remnawave_api.get_all_vless_keys_for_host()`

2. **Функция `get_all_vless_keys_for_host()` в Remnawave API:**
   - Получает всех пользователей хоста через `list_users()`
   - Для каждого пользователя извлекает `subscriptionUrl`
   - Возвращает список всех subscription URLs

3. **Форматирование и вывод:**
   - Все VLESS ключи оборачиваются в `<code>...</code>` теги
   - Нумеруются (1. 2. 3. ...)
   - Выводятся в конце информации о ключе

### Почему используются Subscription URLs?

- `subscriptionUrl` это уже готовый raw VLESS конфиг
- Его можно скопировать и вставить прямо в VPN клиент
- Это актуальный список всех пользователей, загружаемый из Remnawave
- Не требует дополнительных полей в БД

---

## 🔍 Примеры логирования

При использовании функции выведения VLESS, в логах вы увидите:

```
[INFO] Remnawave: получаю VLESS ключи для хоста server-eu-1...
[INFO] Remnawave: найдено 3 VLESS ключей для хоста server-eu-1
[INFO] Ключ шифрован и выведен пользователю
```

При ошибках:

```
[ERROR] Remnawave: ошибка получения VLESS ключей для хоста server-eu-1: Connection timeout
[WARNING] Remnawave: не удалось получить VLESS ключи, но информация о ключе выведена
```

---

**Разработано:** 14 февраля 2026 г.
**Статус:** ✅ Полностью реализовано и готово к использованию


# Реализация переключателя франшизы

## Что было изменено

Добавлена функция включения/выключения франшизы (создание клонов ботов) в Telegram-боте с хранением состояния в SQLite базе данных.

### Измененные файлы:

1. **[src/shop_bot/webhook_server/app.py](src/shop_bot/webhook_server/app.py)**
   - Добавлены функции `franchise_settings()` и `toggle_franchise_settings()`
   - Функции создают таблицу `settings` если её нет
   - Сохраняют состояние франшизы в БД с ключом `franchise_enabled`

2. **[src/shop_bot/bot/admin_handlers.py](src/shop_bot/bot/admin_handlers.py)**
   - Добавлен импорт функций франшизы из `app.py`
   - Добавлены обработчики для меню франшизы:
     - `AdminFranchise` - состояние FSM
     - `admin_franchise_menu_entry()` - вход в меню
     - `admin_franchise_toggle()` - переключение франшизы
   - Защита доступа через проверку `is_admin()`

3. **[src/shop_bot/bot/keyboards.py](src/shop_bot/bot/keyboards.py)**
   - Добавлен импорт `franchise_settings`
   - Изменено условие для кнопки "Создать бота" на `if show_create_bot and franchise_settings():`
   - Добавлена функция `create_admin_franchise_settings_keyboard()`
   - Добавлена кнопка "🏢 Франшиза" в меню настроек админа

## Как это работает

### Для администратора:
1. Откройте бота и перейдите в "⚙️ Админка"
2. Выберите "⚙️ Настройки"
3. Нажмите "🏢 Франшиза"
4. Переключайте кнопку "🔴 Включить франшизу" / "🟢 Выключить франшизу"
5. Состояние автоматически сохраняется в БД

### Для пользователей:
- Если франшиза **включена** - кнопка "🤖 Создать бота" видна в главном меню
- Если франшиза **выключена** - кнопка скрыта от пользователей

## Безопасность

✅ Все обработчики проверяют `is_admin(callback.from_user.id)`  
✅ Кнопка показывается только в админ-меню  
✅ Даже при попытке отправить callback напрямую - доступ будет запрещён  
✅ Обычные пользователи не могут изменить состояние франшизы

## Развертывание

```bash
# Перейдите в директорию проекта
cd ~/xatabchik  # или путь к вашему проекту

# Пересоберите Docker-контейнер
docker-compose down
docker-compose up -d --build

# Проверьте логи
docker-compose logs -f
```

## Тестирование

1. **Откройте бота** от имени администратора
2. **Перейдите в админку** → **Настройки** → **Франшиза**
3. **Убедитесь**, что кнопка работает и переключает состояние
4. **Откройте бота** от имени обычного пользователя
5. **Проверьте**, видна ли кнопка "🤖 Создать бота" (зависит от состояния франшизы)

## База данных

После первого переключения в таблице `settings` появится запись:

| key | value |
|-----|-------|
| franchise_enabled | 0 или 1 |

- `"0"` = франшиза выключена
- `"1"` = франшиза включена

## Состояние по умолчанию

Если запись в БД не существует, франшиза считается **выключенной** (`"0"`).

## Откат изменений

Если понадобится вернуться к предыдущему поведению (кнопка видна всегда), в файле [src/shop_bot/bot/keyboards.py](src/shop_bot/bot/keyboards.py) измените:

```python
# С этого:
if show_create_bot and franchise_settings():
    builder.button(text="🤖 Создать бота", callback_data="factory_create_bot")

# На это:
if show_create_bot:
    builder.button(text="🤖 Создать бота", callback_data="factory_create_bot")
```

---

**Реализация завершена!** ✅

# 🆔 HOST ID система - Полная реализация

## ✨ Что было добавлено

Каждый хост теперь имеет уникальный числовой ID (`host_id`), что обеспечивает:
- ✅ Более надежные связи в БД между таблицами
- ✅ Лучшую производительность запросов (числовые индексы)
- ✅ Возможность стандартизировать API
- ✅ Защиту от переименования хостов

---

## 🗄️ Изменения в базе данных

### Таблица `xui_hosts`
**Новые колонки:**
- `host_id INTEGER UNIQUE` - уникальный ID хоста
- `created_at TIMESTAMP` - дата создания хоста
- `updated_at TIMESTAMP` - дата последнего обновления

### Таблица `vpn_keys`
**Новая колонка:**
- `host_id INTEGER` - связь с хостом (при миграции заполняется из `host_name`)

### Таблица `plans`
**Новая колонка:**
- `host_id INTEGER` - связь с хостом (при миграции заполняется из `host_name`)

### Индексы
При миграции создаются индексы для производительности:
```sql
CREATE INDEX idx_xui_hosts_host_id ON xui_hosts(host_id)
CREATE INDEX idx_vpn_keys_host_id ON vpn_keys(host_id)
CREATE INDEX idx_plans_host_id ON plans(host_id)
```

---

## 📝 Новые функции

### Для работы с хостами

#### `get_host_id(host_name: str) -> int | None`
Получить ID хоста по его имени.
```python
host_id = get_host_id("my-host")
if host_id:
    print(f"ID хоста: {host_id}")
```

#### `get_host_by_id(host_id: int) -> dict | None`
Получить полную информацию о хосте по его ID.
```python
host = get_host_by_id(123)
if host:
    print(f"Имя хоста: {host['host_name']}")
    print(f"URL: {host['host_url']}")
```

#### `get_host_name_by_id(host_id: int) -> str | None`
Получить имя хоста по его ID.
```python
name = get_host_name_by_id(123)
print(f"Имя хоста: {name}")
```

#### `create_host(...) -> int | None` (обновлено)
Теперь возвращает `host_id` вместо `None`.
```python
host_id = create_host(
    name="new-host",
    url="https://panel.example.com",
    user="",
    passwd="",
    inbound=0,
    subscription_url="https://sub.example.com"
)
print(f"✅ Хост создан с ID: {host_id}")
```

---

### Для работы с тарифами

#### `get_plans_by_host_id(host_id: int) -> list[dict]`
Получить все тарифы по ID хоста.
```python
plans = get_plans_by_host_id(123)
for plan in plans:
    print(f"📋 {plan['plan_name']} - {plan['price']} RUB")
```

---

### Для работы с ключами

#### `get_keys_by_host_id(host_id: int) -> list[dict]`
Получить все VPN-ключи для хоста по его ID.
```python
keys = get_keys_by_host_id(123)
for key in keys:
    print(f"🔑 {key['email']} - {key['status']}")
```

#### `update_key_host_id(key_id: int, new_host_id: int) -> bool`
Обновить `host_id` для ключа.
```python
success = update_key_host_id(key_id=456, new_host_id=789)
if success:
    print("✅ Ключ перемещен на новый хост")
```

---

## 🔄 Миграция БД

При первом запуске приложения функция `_migrate_host_ids()` автоматически:

1. ✅ Добавляет колонку `host_id` в `xui_hosts` если её нет
2. ✅ Генерирует ID для существующих хостов (начиная с максимального ID + 1)
3. ✅ Добавляет колонку `host_id` в `vpn_keys` если её нет
4. ✅ Связывает ключи с хостами по `host_name` → заполняет `host_id`
5. ✅ Добавляет колонку `host_id` в `plans` если её нет
6. ✅ Связывает тарифы с хостами по `host_name` → заполняет `host_id`
7. ✅ Создает индексы для производительности

**Логирование:**
```
🔄 Генерация host_id для 5 хостов...
✅ Добавлена колонка host_id в таблицу vpn_keys
✅ Обновлено 42 ключей с host_id
✅ Добавлена колонка host_id в таблицу plans
✅ Обновлено 15 планов с host_id
```

---

## 📖 Примеры использования

### Пример 1: Получить информацию о хосте

```python
from src.shop_bot.data_manager.database import (
    get_host_by_id, 
    get_plans_by_host_id, 
    get_keys_by_host_id
)

# Получить информацию о хосте
host = get_host_by_id(123)
if host:
    print(f"🖥 Хост: {host['host_name']}")
    print(f"   URL: {host['host_url']}")
    print(f"   Remnawave: {host['remnawave_base_url']}")
    
    # Получить тарифы
    plans = get_plans_by_host_id(123)
    print(f"   📋 Тарифов: {len(plans)}")
    
    # Получить ключи
    keys = get_keys_by_host_id(123)
    print(f"   🔑 Ключей: {len(keys)}")
```

### Пример 2: Создать новый хост

```python
from src.shop_bot.data_manager.database import (
    create_host,
    update_host_remnawave_settings
)

# Создать хост
host_id = create_host(
    name="production-eu",
    url="https://panel-eu.example.com",
    user="",
    passwd="",
    inbound=0,
    subscription_url="https://sub-eu.example.com"
)

if host_id:
    print(f"✅ Хост создан с ID: {host_id}")
    
    # Настроить Remnawave
    update_host_remnawave_settings(
        "production-eu",
        remnawave_base_url="https://panel-eu.example.com",
        remnawave_api_token="your-token-here",
        squad_uuid="xxxx-xxxx-xxxx-xxxx"
    )
```

### Пример 3: Перемещение ключа на другой хост

```python
from src.shop_bot.data_manager.database import (
    get_key_by_id,
    update_key_host,
    get_host_name_by_id
)

# Получить текущий ключ
key = get_key_by_id(456)
if key:
    print(f"Ключ сейчас на хосте: {key['host_name']}")

# Получить имя нового хоста по ID
new_host_name = get_host_name_by_id(789)
if new_host_name:
    # Переместить ключ
    success = update_key_host(456, new_host_name)
    if success:
        print(f"✅ Ключ перемещен на: {new_host_name}")
```

### Пример 4: Получить все тарифы и ключи хоста

```python
from src.shop_bot.data_manager.database import (
    get_host_id,
    get_plans_by_host_id,
    get_keys_by_host_id
)

# Найти ID хоста по имени
host_id = get_host_id("my-server")
if not host_id:
    print("Хост не найден")
else:
    # Получить тарифы
    plans = get_plans_by_host_id(host_id)
    print(f"📋 Тарифы ({len(plans)}):")
    for plan in plans:
        print(f"  - {plan['plan_name']}: {plan['price']} RUB")
    
    # Получить ключи
    keys = get_keys_by_host_id(host_id)
    print(f"\n🔑 Ключи ({len(keys)}):")
    for key in keys:
        expires = key.get('expire_at', 'Бесконечно')
        print(f"  - {key['email']}: {expires}")
```

---

## 🔗 Совместимость

### Обратная совместимость
✅ Все старые функции с `host_name` продолжают работать:
- `get_host(host_name)`
- `get_plans_for_host(host_name)`
- `update_key_host(key_id, new_host_name)`

### Прямая совместимость
✅ Новые функции с `host_id`:
- `get_host_by_id(host_id)`
- `get_plans_by_host_id(host_id)`
- `get_keys_by_host_id(host_id)`
- `update_key_host_id(key_id, new_host_id)`

Вы можете использовать оба подхода одновременно!

---

## 🚀 Внедрение

### Шаг 1: Запустить приложение
```bash
python -m src.shop_bot
```
Миграция выполнится автоматически в фоне.

### Шаг 2: Проверить логи
```
🔄 Генерация host_id для X хостов...
✅ Обновлено X ключей с host_id
✅ Обновлено X планов с host_id
```

### Шаг 3: Использовать в коде
Начинайте использовать новые функции с `host_id`.

---

## ⚡ Производительность

### Сравнение

| Сценарий | По host_name | По host_id |
|----------|-------------|-----------|
| Получить информацию хоста | O(n) + TRIM сравнение | O(1) индекс |
| Получить тарифы | Полный скан + TRIM | Прямой индекс |
| Получить ключи | Полный скан | Прямой индекс |

host_id обеспечивает лучшую производительность за счет:
- ✅ Первичных индексов на числовых полях
- ✅ Отсутствия строковых операций (TRIM)
- ✅ Более быстрого сравнения целых чисел (vs LIKE/TRIM для текста)

---

## 🔍 Отладка

### Получить статистику host_id

```python
def get_host_stats():
    """Получить статистику по хостам с ID."""
    with sqlite3.connect(DB_FILE) as conn:
        cursor = conn.cursor()
        
        # Хосты с ID
        cursor.execute("SELECT COUNT(*) FROM xui_hosts WHERE host_id IS NOT NULL")
        hosts_with_id = cursor.fetchone()[0]
        
        # Хосты без ID
        cursor.execute("SELECT COUNT(*) FROM xui_hosts WHERE host_id IS NULL")
        hosts_without_id = cursor.fetchone()[0]
        
        # Ключи с host_id
        cursor.execute("SELECT COUNT(*) FROM vpn_keys WHERE host_id IS NOT NULL")
        keys_with_id = cursor.fetchone()[0]
        
        print(f"Хостов с ID: {hosts_with_id}")
        print(f"Хостов без ID: {hosts_without_id}")
        print(f"Ключей с host_id: {keys_with_id}")
```

---

## ✅ Чек-лист миграции

После запуска приложения убедитесь что:

- [ ] Колонка `host_id` создана в `xui_hosts`
- [ ] Колонка `host_id` создана в `vpn_keys`
- [ ] Колонка `host_id` создана в `plans`
- [ ] Существующие хосты получили ID
- [ ] Существующие ключи связаны с хостами (host_id заполнен)
- [ ] Существующие тарифы связаны с хостами (host_id заполнен)
- [ ] Индексы созданы успешно
- [ ] Логи показывают успешное завершение миграции

---

**Дата реализации:** 14 февраля 2026  
**Статус:** ✅ Готово к использованию  
**Обратная совместимость:** ✅ Полная

# Комментарии: `src/shop_bot/factory_bot/runtime.py`

Процессный слот на `ManagedBotsService` для хендлеров клона. Модульного docstring нет.

## `set_service` (7–9)

**Docstring в коде:** нет

```
"""Записать глобальный ManagedBotsService в `_service`."""
```

## `get_service` (11–12)

**Docstring в коде:** нет

```
"""Вернуть текущий `_service` или None, если set_service ещё не вызывали."""
```

# Комментарии: `modules/example_module/db_cleanup.py`

Модульного docstring нет. Хук выгрузки модуля: вызывающий передаёт уже открытый `db_conn`.

## `cleanup` (1–5)

**Docstring в коде:** нет

```
"""Снять таблицу example_module_items и ключи bot_settings LIKE 'example_module_%', затем commit."""
```
